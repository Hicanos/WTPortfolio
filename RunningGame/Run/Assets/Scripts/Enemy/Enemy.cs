using UnityEngine;

public class Enemy : MonoBehaviour
{

    // Enemy는 Tag에 "Enemy"로 설정됨

    // 소환 로직, 애니메이션 로직만 있으면 됨

    // 에너미 배열 리스트 - 캐릭터의 점수에 따라 추가소환됨
    public Enemy[] enemies;

    // 소환은 SetActive(true)로 함


}
